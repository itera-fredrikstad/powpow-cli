## Summary

<!-- What changed and why. -->

## Checks

- [ ] `/verify` passes (lint + typecheck + tests)
- [ ] `/scratch-build` passes — required if this PR touches `src/build.ts` or `src/plugin/**`
- [ ] `CLAUDE.md` and/or `.claude/docs/*` updated if the change affects architecture, the build state machine, or module resolution

## Notes

<!-- Anything reviewers should pay attention to (gotchas, follow-ups, breaking changes). -->
